/** Pacing — OT Level 5 Session 5 · Moving Eye Control */
export const SESSION5_5_PACING = {
  rounds: 8,
  xpPerScore: 18,
  followDistancePx: 88,
  followHoldMs: 3000,
  followCheckMs: 100,
  fingerRadius: 18,
  nextRoundDelayMs: 500,
} as const;
