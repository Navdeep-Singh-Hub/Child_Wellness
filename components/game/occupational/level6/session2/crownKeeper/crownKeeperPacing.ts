export const CROWN_KEEPER_PACING = {
  calibrationMs: 3000,
  tickMs: 100,
  rounds: 3,
  roundMs: 16000,
  stableBoostMs: 5000,
  stableScoreBoost: 8,
  crownFallPenalty: 15,
  minStabilityToWin: 55,
  nextRoundDelayMs: 900,
  roundIntroDelayMs: 800,
  fallbackHoldMs: 6000,
} as const;
